const BOOKINGS= require('../models/bookingModel');


const getAssignedServices = (req, res) => {
try {
    
} catch (error) {
    console.error(error);
    res.status(500).json({message:'Server Error'})
}
}